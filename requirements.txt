# Lingua — standalone language-learning app.
# Intentionally separate from the root/apps/core requirements: this app ships
# and deploys independently of ARIA.
fastapi==0.135.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
httpx==0.27.2
python-multipart==0.0.32

# Durable production storage (Supabase Postgres) — see backend/db.py
psycopg[binary]==3.2.3

# Open Educational Resources retrieval (backend/oer/) — vector store for
# grounding Academy course generation in real OER sources. Embeddings
# themselves go through the HF Inference API (see hf_client.embed_texts),
# so this stays the only new dependency for the whole RAG pipeline.
chromadb==1.5.9

# Fallback path for persona voices (backend/hf_client.py PARLER_SPACE_ID) —
# calls a verified community Space running Parler-TTS when the direct
# serverless model endpoint isn't hosted. Lightweight, pure-Python client.
gradio_client==2.6.0

# Test suite
pytest==8.3.3
