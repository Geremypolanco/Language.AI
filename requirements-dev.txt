# Test-only tooling for Lingua — never installed into the production Docker
# image (Dockerfile installs requirements.txt alone). Install both files for
# local development: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
